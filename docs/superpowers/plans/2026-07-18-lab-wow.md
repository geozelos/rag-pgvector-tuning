# Lab-Wow Implementation Plan

> **For agentic workers:** Executed 2026-07-18 in-session.

**Goal:** Zero-key latency × recall demo that GitHub visitors can reproduce with `make demo`.

**Architecture:** Feature-hash demo embeddings + topical seed corpus + `scripts/demo_latency_recall.py` sweep writing ASCII/PNG chart; README leads with that chart.

**Tech Stack:** Python, FastAPI, pgvector, httpx, optional Pillow

## Global Constraints

- No embedding API keys required for default path
- Guardrails: `min_ef_search` 16, `max_ef_search` 200

## Tasks (done)

- [x] Replace RNG hash embeddings with signed feature hashing
- [x] Enrich seed corpus with distinct topic vocabularies
- [x] Add `demo_latency_recall.py` + `make demo`
- [x] Rewrite README hero + real CI badge
- [x] Unit tests for embeddings + dry-run demo script
